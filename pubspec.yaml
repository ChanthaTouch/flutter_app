# Step 2: Update pubspec.yaml (no extra dependencies needed)
name: sales_performance_system
description: "Sales Performance System - Full Flutter Frontend"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true